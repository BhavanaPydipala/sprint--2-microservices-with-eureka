spring:
  application:
    name: GetDocumentByName-Service
server:
  port: 1502
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
  instance:
    preferIpAddress: true